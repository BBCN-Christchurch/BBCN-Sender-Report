# For LOCAL testing only. Copy this to ".env" (no ".example") and paste
# your token in. Never commit the real ".env" file.
#
# When hosted on GitHub, the token lives as a repository Secret instead
# (Settings -> Secrets and variables -> Actions) — see README.md.
SENDER_API_TOKEN=paste-your-sender-net-api-token-here
